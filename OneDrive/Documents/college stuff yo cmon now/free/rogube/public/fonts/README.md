# Fonts Directory

Place your .ttf font file here and name it `your-font-name.ttf` (or update the filename in `index.html`).

Then update the @font-face rule in `index.html` to match your font filename.
